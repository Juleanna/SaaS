# 🪟 Windows Setup Guide - Запуск без Docker

## 📋 Зміст

1. [Вступ](#-вступ)
2. [Системні вимоги](#-системні-вимоги)
3. [Крок 1: Встановлення Python](#-крок-1-встановлення-python)
4. [Крок 2: Встановлення Node.js](#-крок-2-встановлення-nodejs)
5. [Крок 3: Встановлення PostgreSQL](#-крок-3-встановлення-postgresql)
6. [Крок 4: Встановлення Redis](#-крок-4-встановлення-redis)
7. [Крок 5: Налаштування Backend](#-крок-5-налаштування-backend)
8. [Крок 6: Налаштування Frontend](#-крок-6-налаштування-frontend)
9. [Крок 7: Запуск проекту](#-крок-7-запуск-проекту)
10. [Troubleshooting](#-troubleshooting)
11. [FAQ](#-faq)

---

## 🎯 Вступ

Ця інструкція допоможе вам **запустити SaaS Mini-Store Platform на Windows** без використання Docker. Всі компоненти будуть встановлені та запущені безпосередньо на вашій системі.

### Що ми встановимо:

```
┌─────────────────────────────────────────────┐
│  🪟 Windows System                          │
├─────────────────────────────────────────────┤
│                                             │
│  🐍 Python 3.11     ⚛️  Node.js 20         │
│  🐘 PostgreSQL 15   📦 Redis 7             │
│                                             │
│  ┌─────────────┐    ┌─────────────┐        │
│  │ 🔧 Backend  │    │ ⚛️  Frontend │        │
│  │ Django API  │    │ React+Vite  │        │
│  │ Port: 8000  │    │ Port: 3000  │        │
│  └─────────────┘    └─────────────┘        │
│                                             │
│  ┌─────────────┐    ┌─────────────┐        │
│  │ 👷 Celery   │    │ ⏰ Celery   │        │
│  │ Worker      │    │ Beat        │        │
│  └─────────────┘    └─────────────┘        │
│                                             │
└─────────────────────────────────────────────┘
```

### Переваги локального запуску:

✅ **Швидша розробка** - Немає overhead від Docker
✅ **Простіший debugging** - Прямий доступ до процесів
✅ **Знайомі інструменти** - Використання звичних IDE
✅ **Менше ресурсів** - Не потрібна віртуалізація

---

## 💻 Системні вимоги

### Мінімальні вимоги:

| Компонент | Вимога |
|-----------|--------|
| **ОС** | Windows 10/11 (64-bit) |
| **RAM** | 8 GB |
| **CPU** | 4 cores |
| **Диск** | 10 GB вільного місця |
| **Права** | Права адміністратора для встановлення |

### Що буде встановлено:

- 🐍 **Python 3.11.x** (~100 MB)
- ⚛️  **Node.js 20.x LTS** (~50 MB)
- 🐘 **PostgreSQL 15.x** (~300 MB)
- 📦 **Redis 7.x** (Windows version) (~15 MB)
- 📦 **Python packages** (~500 MB)
- 📦 **Node packages** (~200 MB)

**Загальний обсяг**: ~1.2 GB

---

## 🐍 Крок 1: Встановлення Python

### 1.1. Перевірка наявності Python

Відкрийте **PowerShell** або **Command Prompt**:

```powershell
python --version
```

**Якщо бачите `Python 3.11.x`** - пропустіть цей крок ✅

**Якщо команда не знайдена або версія інша** - продовжуйте установку ⬇️

### 1.2. Завантаження Python

```
┌────────────────────────────────────────┐
│  Крок 1: Відкрити браузер             │
│  Крок 2: Перейти на python.org        │
│  Крок 3: Downloads → Windows          │
│  Крок 4: Завантажити Python 3.11.x    │
└────────────────────────────────────────┘
```

**Пряме посилання**: https://www.python.org/downloads/windows/

Завантажте **"Windows installer (64-bit)"** для Python 3.11.x

### 1.3. Встановлення Python

**Крок 1: Запустити інсталятор** (`python-3.11.x-amd64.exe`)

```
┌──────────────────────────────────────────┐
│   Python 3.11.x Setup                    │
├──────────────────────────────────────────┤
│                                          │
│  ⚠️  ВАЖЛИВО! Увімкніть обидва:         │
│                                          │
│  ☑️ Install launcher for all users      │
│  ☑️ Add python.exe to PATH ⭐⭐⭐        │
│     ↑↑↑ ЦЕ ОБОВ'ЯЗКОВО!!!              │
│                                          │
│  [ Install Now ]  [ Customize ]          │
└──────────────────────────────────────────┘
```

**КРИТИЧНО ВАЖЛИВО**: ✅ **Add python.exe to PATH** - обов'язково відмітити!

**Крок 2: Натиснути "Install Now"**

Дочекайтесь завершення (~2-3 хвилини):

```
Installing:
  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 100%

✅ Setup was successful
```

**Крок 3: Перевірити установку**

Відкрийте **НОВИЙ** PowerShell (обов'язково новий!):

```powershell
python --version
```

**Очікуваний вивід**:
```
Python 3.11.x
```

```powershell
pip --version
```

**Очікуваний вивід**:
```
pip 24.x.x from C:\...\python311\site-packages\pip (python 3.11)
```

✅ **Python встановлено успішно!**

### 1.4. Оновлення pip

```powershell
python -m pip install --upgrade pip
```

---

## ⚛️ Крок 2: Встановлення Node.js

### 2.1. Перевірка наявності Node.js

```powershell
node --version
```

**Якщо бачите `v20.x.x`** - пропустіть цей крок ✅

### 2.2. Завантаження Node.js

**Посилання**: https://nodejs.org/

Завантажте **LTS версію** (20.x.x)

```
┌────────────────────────────────────────┐
│  Node.js Website                       │
├────────────────────────────────────────┤
│                                        │
│  🟢 20.x.x LTS                         │
│     Recommended For Most Users         │
│     [ Download for Windows (x64) ]     │
│                                        │
│  ⚡ 21.x.x Current                     │
│     Latest Features                    │
└────────────────────────────────────────┘
```

Натисніть **"Download for Windows (x64)"** для LTS версії

### 2.3. Встановлення Node.js

**Крок 1: Запустити інсталятор** (`node-v20.x.x-x64.msi`)

```
┌──────────────────────────────────────────┐
│   Node.js Setup Wizard                   │
├──────────────────────────────────────────┤
│                                          │
│  Welcome to the Node.js Setup Wizard    │
│                                          │
│  This will install Node.js and npm      │
│  on your computer.                       │
│                                          │
│  [ Next ]  [ Cancel ]                    │
└──────────────────────────────────────────┘
```

**Крок 2: Прийняти ліцензію** → Next

**Крок 3: Destination Folder** → залишити за замовчуванням → Next

```
C:\Program Files\nodejs\
```

**Крок 4: Custom Setup**

```
┌──────────────────────────────────────────┐
│   Custom Setup                           │
├──────────────────────────────────────────┤
│                                          │
│  ☑️ Node.js runtime                     │
│  ☑️ npm package manager                 │
│  ☑️ Online documentation shortcuts      │
│  ☑️ Add to PATH ⭐                       │
│                                          │
│  [ Next ]  [ Back ]  [ Cancel ]          │
└──────────────────────────────────────────┘
```

Все має бути відмічено → **Next**

**Крок 5: Tools for Native Modules**

```
┌──────────────────────────────────────────┐
│   Tools for Native Modules               │
├──────────────────────────────────────────┤
│                                          │
│  ☑️ Automatically install necessary tools│
│     (Python, Visual Studio Build Tools) │
│                                          │
│  ⚠️  Рекомендуємо УВІМКНУТИ             │
│                                          │
│  [ Next ]  [ Back ]  [ Cancel ]          │
└──────────────────────────────────────────┘
```

✅ **Увімкніть цю опцію** якщо не маєте Visual Studio Build Tools

**Крок 6: Install** → Дочекайтесь завершення (~3-5 хвилин)

### 2.4. Перевірити установку

Відкрийте **НОВИЙ** PowerShell:

```powershell
node --version
```

**Очікуваний вивід**:
```
v20.x.x
```

```powershell
npm --version
```

**Очікуваний вивід**:
```
10.x.x
```

✅ **Node.js встановлено успішно!**

---

## 🐘 Крок 3: Встановлення PostgreSQL

### 3.1. Завантаження PostgreSQL

**Посилання**: https://www.postgresql.org/download/windows/

Або прямо: https://www.enterprisedb.com/downloads/postgres-postgresql-downloads

Завантажте **PostgreSQL 15.x** для Windows x86-64

### 3.2. Встановлення PostgreSQL

**Крок 1: Запустити інсталятор** (`postgresql-15.x-windows-x64.exe`)

```
┌──────────────────────────────────────────┐
│   PostgreSQL Setup                       │
├──────────────────────────────────────────┤
│                                          │
│  Welcome to PostgreSQL Setup Wizard     │
│                                          │
│  Version: 15.x                           │
│                                          │
│  [ Next ]  [ Cancel ]                    │
└──────────────────────────────────────────┘
```

**Next** →

**Крок 2: Installation Directory**

```
C:\Program Files\PostgreSQL\15
```

Залишити за замовчуванням → **Next**

**Крок 3: Select Components**

```
┌──────────────────────────────────────────┐
│   Select Components                      │
├──────────────────────────────────────────┤
│                                          │
│  ☑️ PostgreSQL Server                   │
│  ☑️ pgAdmin 4 (GUI tool)                │
│  ☑️ Stack Builder (optional)            │
│  ☑️ Command Line Tools ⭐                │
│                                          │
│  [ Next ]  [ Back ]  [ Cancel ]          │
└──────────────────────────────────────────┘
```

Все відмічено → **Next**

**Крок 4: Data Directory**

```
C:\Program Files\PostgreSQL\15\data
```

Залишити за замовчуванням → **Next**

**Крок 5: Password**

```
┌──────────────────────────────────────────┐
│   Password                               │
├──────────────────────────────────────────┤
│                                          │
│  Please provide a password for the       │
│  database superuser (postgres)           │
│                                          │
│  Password: ┌────────────────────────┐    │
│            │ postgres               │    │
│            └────────────────────────┘    │
│                                          │
│  ⚠️  ЗАПАМ'ЯТАЙТЕ цей пароль!           │
│     (Для розробки використовуйте:        │
│      postgres / postgres)                │
│                                          │
│  [ Next ]  [ Back ]  [ Cancel ]          │
└──────────────────────────────────────────┘
```

**Для розробки рекомендуємо**: `postgres`

⚠️ **ВАЖЛИВО**: Запишіть цей пароль!

**Крок 6: Port**

```
Port: 5432
```

Залишити за замовчуванням → **Next**

**Крок 7: Locale**

```
Default locale
```

Залишити за замовчуванням → **Next**

**Крок 8: Summary** → **Next** → **Install**

Дочекайтесь завершення (~5-7 хвилин):

```
Installing PostgreSQL...
  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓ 100%

✅ Installation Complete
```

**Крок 9: Зняти галочку "Launch Stack Builder"** → **Finish**

### 3.3. Додати PostgreSQL в PATH

**Крок 1: Відкрити Environment Variables**

```
1. Натиснути Win + R
2. Ввести: sysdm.cpl
3. Вкладка "Advanced"
4. Кнопка "Environment Variables"
```

**Крок 2: Редагувати Path**

```
┌──────────────────────────────────────────┐
│   Environment Variables                  │
├──────────────────────────────────────────┤
│  User variables:                         │
│    Variable    Value                     │
│    Path        ...                       │
│                                          │
│  [ Edit ]  [ New ]  [ Delete ]           │
├──────────────────────────────────────────┤
│  System variables:                       │
│    Variable    Value                     │
│    Path        ...                       │
│                                          │
│  [ Edit ]  [ New ]  [ Delete ]           │
└──────────────────────────────────────────┘
```

Виберіть **System variables** → **Path** → **Edit**

**Крок 3: Додати шлях**

Натисніть **New** та додайте:

```
C:\Program Files\PostgreSQL\15\bin
```

**OK** → **OK** → **OK**

### 3.4. Перевірити установку

Відкрийте **НОВИЙ** PowerShell:

```powershell
psql --version
```

**Очікуваний вивід**:
```
psql (PostgreSQL) 15.x
```

### 3.5. Створити базу даних

```powershell
# Підключитись до PostgreSQL
psql -U postgres
```

Введіть пароль (postgres)

**У PostgreSQL prompt**:

```sql
-- Створити базу даних
CREATE DATABASE saas_platform;

-- Перевірити
\l

-- Вийти
\q
```

**Вивід має показати**:

```
                                   List of databases
     Name      |  Owner   | Encoding |   Collate   |    Ctype
---------------+----------+----------+-------------+-------------
 saas_platform | postgres | UTF8     | en_US.UTF-8 | en_US.UTF-8
 postgres      | postgres | UTF8     | en_US.UTF-8 | en_US.UTF-8
```

✅ **PostgreSQL встановлено та БД створено!**

---

## 📦 Крок 4: Встановлення Redis

Redis офіційно не підтримує Windows, але є порт від Microsoft.

### 4.1. Завантаження Redis (Windows Port)

**Варіант 1: GitHub Release (Рекомендовано)**

Посилання: https://github.com/tporadowski/redis/releases

Завантажте **Redis-x64-x.x.xxx.zip** (останню версію)

**Варіант 2: Chocolatey**

Якщо маєте Chocolatey:

```powershell
choco install redis-64
```

### 4.2. Встановлення Redis (Manual)

**Крок 1: Розпакувати ZIP**

Розпакуйте в:
```
C:\Redis\
```

Структура має бути:

```
C:\Redis\
  ├── redis-server.exe
  ├── redis-cli.exe
  ├── redis.windows.conf
  └── ...
```

**Крок 2: Додати в PATH**

1. Win + R → `sysdm.cpl`
2. Advanced → Environment Variables
3. System variables → Path → Edit → New
4. Додати: `C:\Redis`
5. OK → OK → OK

**Крок 3: Встановити як Windows Service (Опціонально)**

Відкрийте PowerShell **як адміністратор**:

```powershell
cd C:\Redis

# Встановити сервіс
.\redis-server.exe --service-install redis.windows.conf --loglevel verbose

# Запустити сервіс
.\redis-server.exe --service-start
```

### 4.3. Запуск Redis (Manual Mode)

**Якщо НЕ встановили як сервіс**, запускайте вручну:

Відкрийте **нове вікно PowerShell**:

```powershell
cd C:\Redis
.\redis-server.exe
```

**Має з'явитись**:

```
                _._
           _.-``__ ''-._
      _.-``    `.  `_.  ''-._           Redis 7.x.x (windows port)
  .-`` .-```.  ```\/    _.,_ ''-._
 (    '      ,       .-`  | `,    )     Running in standalone mode
 |`-._`-...-` __...-.``-._|'` _.-'|     Port: 6379
 |    `-._   `._    /     _.-'    |     PID: xxxxx
  `-._    `-._  `-./  _.-'    _.-'
 |`-._`-._    `-.__.-'    _.-'_.-'|
 |    `-._`-._        _.-'_.-'    |
  `-._    `-._`-.__.-'_.-'    _.-'
      `-._    `-.__.-'    _.-'
          `-._        _.-'
              `-.__.-'

[xxxxx] Server started, Redis version 7.x.x
[xxxxx] Server is now ready to accept connections on port 6379
```

⚠️ **НЕ ЗАКРИВАЙТЕ це вікно** - Redis працює!

### 4.4. Перевірити Redis

Відкрийте **нове вікно PowerShell**:

```powershell
redis-cli ping
```

**Очікуваний вивід**:
```
PONG
```

✅ **Redis встановлено та працює!**

### 4.5. Автозапуск Redis (Service Mode)

Якщо встановили як сервіс, перевірити:

```powershell
# Статус
Get-Service redis

# Зупинити
Stop-Service redis

# Запустити
Start-Service redis

# Автозапуск
Set-Service redis -StartupType Automatic
```

---

## 🔧 Крок 5: Налаштування Backend

### 5.1. Перейти в папку проекту

```powershell
cd d:\SaaS
```

### 5.2. Створити віртуальне середовище

```powershell
# Перейти в папку backend
cd backend

# Створити venv
python -m venv venv
```

**Структура після створення**:

```
backend/
  ├── venv/              ← Створено!
  │   ├── Scripts/
  │   ├── Lib/
  │   └── pyvenv.cfg
  ├── manage.py
  ├── requirements.txt
  └── ...
```

### 5.3. Активувати віртуальне середовище

```powershell
# PowerShell
.\venv\Scripts\Activate.ps1
```

**Якщо помилка "running scripts is disabled"**:

```powershell
# Змінити execution policy
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Спробувати знову
.\venv\Scripts\Activate.ps1
```

**Після активації побачите**:

```powershell
(venv) PS D:\SaaS\backend>
      ↑↑↑↑↑
    Активовано!
```

### 5.4. Встановити залежності

```powershell
# Оновити pip
python -m pip install --upgrade pip

# Встановити всі пакети
pip install -r requirements.txt
```

**Процес встановлення** (~3-5 хвилин):

```
Collecting Django==5.2.4
  Downloading Django-5.2.4-py3-none-any.whl (8.2 MB)
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 8.2/8.2 MB 5.0 MB/s

Collecting djangorestframework==3.14.0
  Downloading djangorestframework-3.14.0-py3-none-any.whl (1.1 MB)
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 1.1/1.1 MB 6.2 MB/s

... (45 packages total)

Successfully installed Django-5.2.4 djangorestframework-3.14.0 celery-5.3.4 ...
```

### 5.5. Створити .env файл

```powershell
# Створити .env
New-Item -Path ".env" -ItemType File

# Відкрити в блокноті
notepad .env
```

**Вставити наступний вміст**:

```bash
# Django Core
DEBUG=True
SECRET_KEY=dev-secret-key-for-local-development-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=saas_platform
DB_USER=postgres
DB_PASSWORD=postgres

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Instagram (опціонально)
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
INSTAGRAM_REDIRECT_URI=http://localhost:8000/api/instagram/oauth/callback

# Telegram (опціонально)
TELEGRAM_BOT_TOKEN=
```

**Зберегти** (Ctrl+S) та **закрити**

### 5.6. Застосувати міграції

```powershell
python manage.py migrate
```

**Очікуваний вивід**:

```
Operations to perform:
  Apply all migrations: admin, auth, contenttypes, sessions, accounts, stores, products, orders, core
Running migrations:
  Applying contenttypes.0001_initial... OK
  Applying auth.0001_initial... OK
  Applying accounts.0001_initial... OK
  Applying admin.0001_initial... OK
  ... (23 migrations total)

✅ All migrations applied successfully!
```

### 5.7. Створити суперюзера

```powershell
python manage.py createsuperuser
```

**Інтерактивний prompt**:

```
Username: admin
Email address: admin@example.com
Password: admin
Password (again): admin

Superuser created successfully.
```

### 5.8. Зібрати статичні файли

```powershell
python manage.py collectstatic --noinput
```

**Вивід**:

```
124 static files copied to 'D:\SaaS\backend\staticfiles'.
```

✅ **Backend налаштовано успішно!**

---

## ⚛️ Крок 6: Налаштування Frontend

### 6.1. Перейти в папку frontend

```powershell
# З кореня проекту
cd ..\frontend-vite
```

### 6.2. Встановити залежності

```powershell
npm install
```

**Процес встановлення** (~2-3 хвилини):

```
npm WARN deprecated ...

added 256 packages, and audited 257 packages in 2m

34 packages are looking for funding
  run `npm fund` for details

found 0 vulnerabilities
```

### 6.3. Створити .env файл (опціонально)

```powershell
# Створити .env
New-Item -Path ".env" -ItemType File

# Відкрити
notepad .env
```

**Вміст**:

```bash
VITE_API_URL=http://localhost:8000/api
VITE_WS_URL=ws://localhost:8000
```

**Зберегти** та **закрити**

✅ **Frontend налаштовано успішно!**

---

## 🚀 Крок 7: Запуск проекту

Тепер запустимо всі компоненти. **Потрібно 5 окремих вікон терміналу!**

### Архітектура запуску:

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  🪟 Terminal 1      🪟 Terminal 2                   │
│  🐘 PostgreSQL      📦 Redis                        │
│  (сервіс/фон)       (сервіс/фон)                    │
│                                                     │
│  🪟 Terminal 3      🪟 Terminal 4                   │
│  🔧 Backend         👷 Celery Worker                │
│  Port: 8000                                         │
│                                                     │
│  🪟 Terminal 5      🪟 Terminal 6                   │
│  ⚛️  Frontend       ⏰ Celery Beat                  │
│  Port: 3000                                         │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 7.1. Terminal 1: PostgreSQL

**Якщо PostgreSQL як сервіс** - він вже працює ✅

Перевірити:

```powershell
Get-Service postgresql*
```

**Якщо не запущений**:

```powershell
Start-Service postgresql-x64-15
```

### 7.2. Terminal 2: Redis

**Якщо Redis як сервіс**:

```powershell
# Перевірити
Get-Service redis

# Запустити якщо потрібно
Start-Service redis
```

**Якщо Redis manual mode**:

```powershell
cd C:\Redis
.\redis-server.exe
```

Залишити вікно **відкритим**!

### 7.3. Terminal 3: Django Backend

```powershell
# Перейти в backend
cd d:\SaaS\backend

# Активувати venv
.\venv\Scripts\Activate.ps1

# Запустити сервер
python manage.py runserver
```

**Очікуваний вивід**:

```
Watching for file changes with StatReloader
Performing system checks...

System check identified no issues (0 silenced).
February 12, 2026 - 14:30:00
Django version 5.2.4, using settings 'core.settings'
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

```
┌────────────────────────────────────────┐
│  🟢 Backend is running!                │
│  📍 http://localhost:8000              │
│  📍 Admin: /admin                      │
│  📍 API Docs: /api/docs                │
└────────────────────────────────────────┘
```

⚠️ **НЕ ЗАКРИВАЙТЕ це вікно!**

### 7.4. Terminal 4: Celery Worker

**Відкрити НОВЕ вікно PowerShell**:

```powershell
# Перейти в backend
cd d:\SaaS\backend

# Активувати venv
.\venv\Scripts\Activate.ps1

# Запустити Celery worker
celery -A core worker -l info --pool=solo
```

**ℹ️ Важливо**: На Windows використовуємо `--pool=solo`

**Очікуваний вивід**:

```
 -------------- celery@DESKTOP-XXX v5.3.4 (emerald-rush)
--- ***** -----
-- ******* ---- Windows-10-10.0.19045-SP0 2026-02-12 14:31:00
- *** --- * ---
- ** ---------- [config]
- ** ---------- .> app:         core:0x1234567890
- ** ---------- .> transport:   redis://localhost:6379/0
- ** ---------- .> results:     redis://localhost:6379/0
- *** --- * --- .> concurrency: 1 (solo)
-- ******* ---- .> task events: OFF
--- ***** -----
 -------------- [queues]
                .> celery           exchange=celery(direct) key=celery

[tasks]
  . core.instagram_tasks.sync_instagram_posts
  . core.instagram_tasks.process_instagram_dm_message
  . core.instagram_tasks.refresh_expiring_instagram_tokens

[2026-02-12 14:31:00,000: INFO/MainProcess] Connected to redis://localhost:6379/0
[2026-02-12 14:31:00,010: INFO/MainProcess] celery@DESKTOP-XXX ready.
```

```
┌────────────────────────────────────────┐
│  🟢 Celery Worker is ready!            │
│  👷 Concurrency: 1 (solo)              │
│  📋 Tasks: 3 registered                │
└────────────────────────────────────────┘
```

⚠️ **НЕ ЗАКРИВАЙТЕ це вікно!**

### 7.5. Terminal 5: Celery Beat (Scheduler)

**Відкрити НОВЕ вікно PowerShell**:

```powershell
# Перейти в backend
cd d:\SaaS\backend

# Активувати venv
.\venv\Scripts\Activate.ps1

# Запустити Celery beat
celery -A core beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
```

**Очікуваний вивід**:

```
celery beat v5.3.4 (emerald-rush) is starting.
__    -    ... __   -        _
LocalTime -> 2026-02-12 14:32:00
Configuration ->
    . broker -> redis://localhost:6379/0
    . loader -> celery.loaders.app.AppLoader
    . scheduler -> django_celery_beat.schedulers.DatabaseScheduler

    . logfile -> [stderr]@%INFO
    . maxinterval -> 5.00 seconds (5s)
[2026-02-12 14:32:00,000: INFO/MainProcess] beat: Starting...
[2026-02-12 14:32:00,010: INFO/MainProcess] DatabaseScheduler: Schedule changed.
```

```
┌────────────────────────────────────────┐
│  🟢 Celery Beat is running!            │
│  ⏰ Scheduler: DatabaseScheduler       │
└────────────────────────────────────────┘
```

⚠️ **НЕ ЗАКРИВАЙТЕ це вікно!**

### 7.6. Terminal 6: React Frontend

**Відкрити НОВЕ вікно PowerShell**:

```powershell
# Перейти в frontend
cd d:\SaaS\frontend-vite

# Запустити dev server
npm run dev
```

**Очікуваний вивід**:

```
> frontend-vite@0.0.0 dev
> vite


  VITE v5.0.8  ready in 892 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

```
┌────────────────────────────────────────┐
│  🟢 Frontend is running!               │
│  📍 http://localhost:3000              │
│  ⚡ HMR enabled (hot reload)           │
└────────────────────────────────────────┘
```

⚠️ **НЕ ЗАКРИВАЙТЕ це вікно!**

---

## ✅ Перевірка роботи

### Всі 6 вікон мають бути відкриті:

```
✅ Terminal 1: PostgreSQL (service/background)
✅ Terminal 2: Redis (service or redis-server.exe)
✅ Terminal 3: Django Backend (runserver)
✅ Terminal 4: Celery Worker (worker)
✅ Terminal 5: Celery Beat (beat)
✅ Terminal 6: Vite Frontend (npm run dev)
```

### Відкрити у браузері:

#### 1️⃣ Frontend

**URL**: http://localhost:3000

```
┌──────────────────────────────────────┐
│  🏠 SaaS Mini-Store Platform         │
├──────────────────────────────────────┤
│                                      │
│  Головна сторінка React додатку      │
│  Має завантажитись інтерфейс         │
│                                      │
└──────────────────────────────────────┘
```

#### 2️⃣ Backend API

**URL**: http://localhost:8000/api

Має показати JSON з доступними endpoints

#### 3️⃣ Admin Panel

**URL**: http://localhost:8000/admin

```
┌──────────────────────────────────────┐
│  🔐 Django Administration            │
├──────────────────────────────────────┤
│  Username: admin                     │
│  Password: admin                     │
│  [ Log in ]                          │
└──────────────────────────────────────┘
```

Логін: `admin` / Пароль: `admin`

#### 4️⃣ API Documentation

**URL**: http://localhost:8000/api/docs

Swagger UI з інтерактивною документацією API

---

## 🎉 Проект запущено успішно!

```
┌──────────────────────────────────────────────┐
│  ✅ Всі сервіси працюють!                    │
├──────────────────────────────────────────────┤
│  🐘 PostgreSQL:  Port 5432  ✓               │
│  📦 Redis:       Port 6379  ✓               │
│  🔧 Backend:     Port 8000  ✓               │
│  ⚛️  Frontend:    Port 3000  ✓               │
│  👷 Celery:      Running    ✓               │
│  ⏰ Beat:        Running    ✓               │
│                                              │
│  📍 Open: http://localhost:3000              │
└──────────────────────────────────────────────┘
```

---

## 🛑 Зупинка проекту

### Коректна зупинка:

1. **Celery Beat** (Terminal 5): `Ctrl + C`
2. **Celery Worker** (Terminal 4): `Ctrl + C`
3. **Frontend** (Terminal 6): `Ctrl + C`
4. **Backend** (Terminal 3): `Ctrl + Pause/Break`

**Якщо Redis запущено вручну**:
5. **Redis** (Terminal 2): `Ctrl + C`

**PostgreSQL** залишається як сервіс

### Наступний запуск:

Потрібно запустити тільки 4 термінали (3-6), якщо PostgreSQL та Redis як сервіси.

---

## 🐛 Troubleshooting

### Проблема 1: "python" не розпізнано

**Помилка**:
```
'python' is not recognized as an internal or external command
```

**Рішення**:

1. Перевстановити Python з галочкою "Add to PATH"
2. АБО додати вручну в PATH:
   ```
   C:\Users\YourName\AppData\Local\Programs\Python\Python311\
   C:\Users\YourName\AppData\Local\Programs\Python\Python311\Scripts\
   ```
3. Перезапустити PowerShell

### Проблема 2: "Cannot be loaded because running scripts is disabled"

**Помилка при активації venv**:
```
.\venv\Scripts\Activate.ps1 : File cannot be loaded because running scripts is disabled
```

**Рішення**:

```powershell
# Змінити execution policy
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Спробувати знову
.\venv\Scripts\Activate.ps1
```

### Проблема 3: Port already in use (8000 або 3000)

**Помилка**:
```
Error: That port is already in use.
```

**Рішення**:

```powershell
# Знайти процес на порту 8000
netstat -ano | findstr :8000

# Вбити процес
taskkill /PID <PID> /F

# Або для порту 3000
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Проблема 4: PostgreSQL з'єднання не вдалося

**Помилка**:
```
django.db.utils.OperationalError: could not connect to server
```

**Рішення**:

1. Перевірити чи PostgreSQL запущено:
   ```powershell
   Get-Service postgresql*
   ```

2. Запустити якщо потрібно:
   ```powershell
   Start-Service postgresql-x64-15
   ```

3. Перевірити пароль у `.env`:
   ```
   DB_PASSWORD=postgres
   ```

4. Перевірити з'єднання:
   ```powershell
   psql -U postgres -d saas_platform
   ```

### Проблема 5: Redis не доступний

**Помилка**:
```
Error 10061 connecting to localhost:6379. No connection could be made
```

**Рішення**:

1. Перевірити чи Redis запущено:
   ```powershell
   redis-cli ping
   ```

2. Якщо "Could not connect":
   - Якщо сервіс: `Start-Service redis`
   - Якщо manual: відкрити Terminal і запустити `redis-server.exe`

### Проблема 6: Celery не запускається на Windows

**Помилка**:
```
ValueError: not enough values to unpack
```

**Рішення**:

Використовуйте `--pool=solo`:

```powershell
celery -A core worker -l info --pool=solo
```

### Проблема 7: Міграції не застосовуються

**Помилка**:
```
django.db.migrations.exceptions.InconsistentMigrationHistory
```

**Рішення**:

```powershell
# Видалити БД та створити заново
psql -U postgres
DROP DATABASE saas_platform;
CREATE DATABASE saas_platform;
\q

# Застосувати міграції знову
python manage.py migrate
```

### Проблема 8: Frontend не завантажується

**Помилка у браузері**:
```
This site can't be reached
```

**Рішення**:

1. Перевірити чи запущено Vite:
   ```powershell
   # У терміналі має бути:
   ➜  Local:   http://localhost:3000/
   ```

2. Перевірити firewall - дозволити Node.js

3. Спробувати інший порт:
   ```powershell
   npm run dev -- --port 3001
   ```

### Проблема 9: npm install fails

**Помилка**:
```
npm ERR! code EINTEGRITY
```

**Рішення**:

```powershell
# Очистити npm cache
npm cache clean --force

# Видалити node_modules
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# Встановити знову
npm install
```

### Проблема 10: Access Denied при встановленні

**Рішення**:

Запустити PowerShell **як адміністратор**:

1. Win + X
2. Вибрати "Windows PowerShell (Admin)" або "Terminal (Admin)"

---

## 💡 Корисні команди

### Backend (Django)

```powershell
# Активувати venv
cd d:\SaaS\backend
.\venv\Scripts\Activate.ps1

# Запустити сервер
python manage.py runserver

# Створити міграції
python manage.py makemigrations

# Застосувати міграції
python manage.py migrate

# Django shell
python manage.py shell

# Створити додаток
python manage.py startapp myapp

# Запустити тести
python manage.py test

# Collect static
python manage.py collectstatic
```

### Frontend (React)

```powershell
cd d:\SaaS\frontend-vite

# Запустити dev server
npm run dev

# Build для production
npm run build

# Preview production build
npm run preview

# Lint
npm run lint

# Встановити пакет
npm install package-name

# Видалити пакет
npm uninstall package-name
```

### PostgreSQL

```powershell
# Підключитись до БД
psql -U postgres -d saas_platform

# Backup БД
pg_dump -U postgres saas_platform > backup.sql

# Restore БД
psql -U postgres saas_platform < backup.sql

# Список БД
psql -U postgres -c "\l"
```

### Redis

```powershell
# Підключитись до Redis
redis-cli

# Ping
redis-cli ping

# Очистити всі дані (обережно!)
redis-cli FLUSHALL

# Інформація
redis-cli INFO
```

### Celery

```powershell
cd d:\SaaS\backend
.\venv\Scripts\Activate.ps1

# Worker
celery -A core worker -l info --pool=solo

# Beat
celery -A core beat -l info

# Inspect active tasks
celery -A core inspect active

# Inspect registered tasks
celery -A core inspect registered

# Purge all tasks
celery -A core purge
```

---

## ❓ FAQ

### Q: Чи потрібно кожен раз запускати всі 6 терміналів?

**A**: PostgreSQL та Redis можна налаштувати як Windows сервіси - вони будуть запускатись автоматично. Тоді потрібно тільки 4 термінали: Backend, Frontend, Celery Worker, Celery Beat.

### Q: Як зробити щоб не активувати venv кожен раз?

**A**: Створіть BAT файл `start-backend.bat`:

```batch
@echo off
cd d:\SaaS\backend
call venv\Scripts\activate.bat
python manage.py runserver
```

Аналогічно для Celery.

### Q: Чи можна запустити на Linux/macOS?

**A**: Так! Інструкції дуже схожі, основні відмінності:
- Активація venv: `source venv/bin/activate`
- Redis: офіційна підтримка Linux/macOS

### Q: Як оновити Python пакет?

**A**:

```powershell
# У venv
pip install --upgrade package-name

# Оновити requirements.txt
pip freeze > requirements.txt
```

### Q: Frontend змінюється але не оновлюється у браузері?

**A**: Vite має HMR (Hot Module Replacement) - має оновлюватись автоматично. Якщо ні:
1. Ctrl+R у браузері (hard refresh)
2. Очистити кеш браузера
3. Перезапустити Vite dev server

### Q: Як під'єднатись до БД через GUI?

**A**: Використайте **pgAdmin 4** (встановлюється з PostgreSQL):

1. Відкрити pgAdmin 4
2. Create → Server
3. Name: `Local SaaS`
4. Connection:
   - Host: `localhost`
   - Port: `5432`
   - Database: `saas_platform`
   - Username: `postgres`
   - Password: `postgres`

### Q: Celery task не виконується?

**A**: Перевірити:

1. Worker працює:
   ```powershell
   # Має бути вікно з "celery@DESKTOP-XXX ready"
   ```

2. Redis доступний:
   ```powershell
   redis-cli ping
   # Має бути: PONG
   ```

3. Task зареєстрована:
   ```powershell
   celery -A core inspect registered
   ```

### Q: Як створити production build?

**A**: Для production використовуйте Docker або дивіться [DEPLOYMENT.md](DEPLOYMENT.md).

Коротко:
- Backend: Gunicorn замість runserver
- Frontend: `npm run build` → статичні файли
- PostgreSQL: Окрема база даних
- Redis: Окремий сервер
- Nginx: Reverse proxy

---

## 📚 Додаткові ресурси

- 📖 [Django Documentation](https://docs.djangoproject.com/)
- 📖 [React Documentation](https://react.dev/)
- 📖 [Vite Documentation](https://vitejs.dev/)
- 📖 [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- 📖 [Redis Documentation](https://redis.io/docs/)
- 📖 [Celery Documentation](https://docs.celeryproject.org/)
- 📖 [Docker Guide](DOCKER_GUIDE.md) - Альтернативний спосіб запуску
- 📖 [Deployment Guide](DEPLOYMENT.md) - Production deployment

---

## 🎉 Готово!

Тепер ви можете розробляти проект локально на Windows!

```
┌──────────────────────────────────────────────┐
│  ✅ Local Development Setup Complete!        │
├──────────────────────────────────────────────┤
│  📍 Frontend:    http://localhost:3000       │
│  📍 Backend:     http://localhost:8000       │
│  📍 Admin:       http://localhost:8000/admin │
│  📍 API Docs:    http://localhost:8000/api/docs │
│                                              │
│  🔐 Admin Login: admin / admin               │
└──────────────────────────────────────────────┘
```

**Швидкий старт наступного разу**:

1. Запустити PostgreSQL (якщо не сервіс)
2. Запустити Redis (якщо не сервіс)
3. Backend: `cd backend → .\venv\Scripts\Activate.ps1 → python manage.py runserver`
4. Frontend: `cd frontend-vite → npm run dev`
5. Celery Worker: `cd backend → .\venv\Scripts\Activate.ps1 → celery -A core worker -l info --pool=solo`
6. Celery Beat: `cd backend → .\venv\Scripts\Activate.ps1 → celery -A core beat -l info`

**🚀 Happy coding!**
